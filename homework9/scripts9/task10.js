// 10. Дано масив цін у грн. Податок складає 20%. Сформувати масив, який буде містити величину сплаченого податку у грн.
if (confirm("Почати виведення?")) {
  let prices = [100, 250, 50, 400, 150];
  let taxRate = 0.2;
  let taxes = prices.map((price) => price * taxRate);
  document.write("Масив сплаченого податку у грн: " + taxes);
}
